# Handoff: Top-5 daily gainers → paper-trade batch (deferred from marathon session)

**Created**: 2026-04-23 08:30
**Mode**: proceed
**Repository**: `trading` at `C:\dev\trading`
**Branch**: master is at `ab95478` (PR #47). You are not on master locally — the prior session's transient branch was `security/p0-hardening` (not owned by this work). Start by checking out master fresh.

---

## GOAL

The user asked mid-session: **"возьми 5 топ выросших акций за вчера и добавь их в paper trade по цене закрытия."**

Take the 5 biggest gainers from the latest `reversal_entries` cohort (yesterday's close cohort) and paper-fill them at their `entry_price` (which IS the enrollment-day close — 5 min before market close). Quantity default: `floor($1000 / entry_price)` per ticker, matching the `BatchTradeModal` default. Account: Default (`id=1`).

The user then pivoted to a perf-probe flow before this could be executed, and never explicitly re-requested it. This handoff exists so the ask doesn't get lost. Resume only if the user brings it up — do not execute unprompted.

## CURRENT STATE

**Status**: 12 PRs (#35–#47) merged today, 104/104 tests, prod healthz 200. Paper account `Default` was last reset to **pristine** ($100,000 cash, 0 positions, 0 trades). Matrix virtualization is live and working.

The top-5-gainers flow was **attempted** via headed Playwright in `scripts/run-top5-gainers.mjs` (since deleted per the one-shot-scripts convention). Output was:

```
→ total ticker buttons in gainers view: 586
→ top-5 gainers (latest cohort): NVTS, CAR, AMPX, VMI, ALAB
→ rows checked:
→    NVTS  entry=$15.62  day=+18.3%
→    CAR   entry=$711.14 day=+16.8%
→    AMPX  entry=$22.09  day=+12.6%
→    VMI   entry=$459.02 day=+12.0%
→    ALAB  entry=$196.46 day=+11.8%
→ click 'Paper-trade 5 selected →' CTA
→ modal open — defaults should match matrix entry_price.
→ click Submit
→ (modal showed "Paper-trade 0 selected tickers" — Submit 0 orders)
```

**Root cause of failure**: Playwright `.check()` on the React-controlled F2 checkbox did NOT trigger React's `onChange` handler. The DOM box ticked visually but `selectedRowIds` state stayed empty, so the modal opened with zero rows. **Zero paper trades were created in prod.** The account is still pristine.

Your task is to redo this flow **via direct POST** to `/api/paper/batch-order` — bypass the UI entirely. That path was exercised successfully multiple times today (see PR #40/#41 verification history).

## WORK COMPLETED (earlier today — context only, don't redo)

- 12 PRs merged: #35–#47 (see `git log origin/master -14`)
- Full matrix→paper batch feature live (`/api/paper/batch-order`, `BatchTradeModal`, sticky CTA on `/reversal`)
- Idempotency via `client_request_id`, deviation band (±20%), lazy whitelist sync, `is_manual_fill` provenance
- Matrix perf: 66,883 DOM → 327 (virtualized), wall-load 11.6s → 4.4s
- See `.claude/SYSTEM.md` (243 lines) for topology + API cheatsheet
- See `.claude/agent-log.md` top 5 entries for detailed per-PR context

### Uncommitted Changes
None relevant to this task. Working-tree has two untracked artifacts owned by other tools — ignore:
- `.claude/security-audit-2026-04-22.md`
- `scripts/wake-codex/`

## ARCHITECTURE & FLOW

Direct POST path (the one you'll use):

```
You: POST /api/paper/batch-order?account_id=1
         body: { orders: [{symbol, side, qty, fill_price, client_request_id, ...}] }
              │
              ▼
        Route (src/app/api/paper/batch-order/route.ts)
          1. Zod validate (BatchOrderSchema)
          2. resolveAccount(1) → Default account
          3. filterTradableSymbols([AAPL,...]) — bulk whitelist query
          4. getLastCloseMap([AAPL,...]) — bulk last-close query
          5. Per-row:
             - whitelist check (fail fast)
             - checkFillPriceDeviation (±20% band)
             - idempotency pre-check via LEFT JOIN paper_trades
             - INSERT paper_orders (is_manual_fill=1)
             - fillOrder(pool, orderId, fill_price)  ← bypasses RTH + live-price
          6. Return {summary, results: per-row}
              │
              ▼
        paper_trades row created, /paper UI shows position
```

To get the top-5 gainers for the latest cohort, don't use Yahoo directly — the matrix data (`reversal_entries` table via `/api/reversal`) already HAS them. The cohort day_change_pct captures the magnitude of the move on the enrollment day.

## KEY FILES

| File | Purpose | Key exports/functions |
|------|---------|---------------------|
| `src/app/api/paper/batch-order/route.ts` | POST endpoint to use | `BatchOrderSchema`, POST handler with full JSDoc on order-dependent semantics |
| `src/app/api/reversal/route.ts` | GET to read cohort entries | Accepts `?status=ACTIVE` — gives you just the active enrollments; response shape `{cohorts: Record<date, ReversalEntry[]>}` |
| `src/lib/paper-risk.ts` | Deviation band + whitelist helpers | `checkFillPriceDeviation`, `filterTradableSymbols`, `getLastCloseMap`, `FILL_PRICE_DEVIATION_BAND = 0.2` |
| `src/lib/reversal.ts` | `ReversalEntry` type | Has `symbol`, `direction`, `entry_price`, `day_change_pct`, `cohort_date`, `status`, `consecutive_days` |

### Batch endpoint contract (from route JSDoc)

```typescript
// BatchOrderItemSchema — validated server-side:
{
  symbol: string,            // SYMBOL_RE regex, uppercased
  side: "LONG" | "SHORT",    // → internally maps to BUY+LONG / SELL+SHORT
  qty: number,               // 1..100000, positive
  fill_price: number,        // 0.01..100000, must be within 20% of last close
  stop_loss_pct?: number,    // 0.1..50
  take_profit_pct?: number,  // 0.1..100
  trailing_stop_pct?: number,// 0.1..50
  client_request_id?: string // 8..64 chars [A-Za-z0-9_-], idempotency key
}
// Batch: orders: BatchOrderItemSchema[] with min(1).max(50)
```

## DATABASE

| Table | Relevance | Changes |
|-------|-----------|---------|
| `reversal_entries` | Source of truth for "top gainers" — `day_change_pct` + `status='ACTIVE'` + latest `cohort_date` | Read-only for this task |
| `paper_orders` | Batch INSERTs land here with `is_manual_fill=1` | Writes via `fillOrder()` |
| `paper_trades` | Idempotency replay JOINs on this for real quantity | Writes automatic via fillOrder |
| `tradable_symbols` | Whitelist. Post-PR #38, includes lazy-added rows with `exchange='LAZY_SYNC'` | Read-only |
| `prices_daily` | Deviation band's reference source — `getLastCloseMap` pulls latest close | Read-only |

### Migrations
- All applied. Nothing pending.

## PHILOSOPHY & DECISIONS

| Decision | Rationale | Rejected Alternative |
|----------|-----------|---------------------|
| Use direct POST, not UI | Prior attempt via Playwright `.check()` didn't fire React onChange on F2 checkbox. Direct POST is the verified-working path (PR #40 + #41 E2Es) | Fix the Playwright→React interaction — not worth the debug time |
| Top-5 from `reversal_entries`, not Yahoo | Matrix data is already the curated "interesting movers" set, plus it's cached in our DB and the deviation band's `prices_daily` refs match the enrollment `entry_price` | Live Yahoo fetch — adds network dep, may diverge from enrollment semantics |
| `fill_price = entry.entry_price` | User explicitly asked "по цене закрытия" (at the closing price) and `entry_price` IS the 5-min-before-close snapshot taken at enrollment | `fill_price = today's last close` — different value, breaks the "pretend I bought at matrix price" mental model |
| qty default = `floor($1000 / entry_price)` | Matches the `BatchTradeModal` client-side default, so behaviour is consistent regardless of UI vs API entry point | Fixed qty=1 — too small to test meaningfully; fixed qty=100 — too much cash exposure |
| Use `Promise.allSettled` where orchestrating multiple endpoints | Established today (PR #45) — one arm failing shouldn't tank the other | `Promise.all` — we've been burned by this twice today |
| Reset account to pristine after E2E | User preference expressed multiple times today; paper state stays clean between runs | Leave positions open — clutters UI for next session |

## REMAINING WORK

### Priority Order:

1. **Verify user actually wants this** — before touching prod, confirm: "You asked at [timestamp] to add top-5 gainers from yesterday's cohort. Prior attempt via UI failed; I'll now do it via direct POST. OK to proceed?"

2. **Fetch the top-5 from cohort** — single authenticated GET request:
   ```
   GET /api/reversal?status=ACTIVE
   Cookie: <session from Playwright login or curl with browser cookies>
   ```
   Parse response: `{cohorts: {"2026-04-21": [...], "2026-04-22": [...], ...}}`. The keys are sorted newest-first server-side. Take the first cohort's entries, filter to `day_change_pct > 0` (gainers only), sort descending by `day_change_pct`, slice(0, 5).

3. **Build the batch payload**. Per entry in the top-5:
   ```js
   const batchId = `top5-${Date.now().toString(36)}`;
   const payload = {
     orders: top5.map((e, i) => ({
       symbol: e.symbol,
       side: "LONG", // biggest gainers — paper-trade going with the move
       qty: Math.max(1, Math.floor(1000 / e.entry_price)),
       fill_price: Number(e.entry_price.toFixed(4)),
       client_request_id: `${batchId}-${i}`,
       // No stops — user didn't specify; leave bracket fields unset
     }))
   };
   ```
   Note the `side: "LONG"` choice. The matrix directionality for gainers is `"SHORT"` (mean-reversion hypothesis: gainers fall back). But the user's ask was "buy them" implicitly ("добавь в paper trade"). If ambiguous → ASK FIRST.

4. **POST to batch endpoint** — same Playwright-authenticated session approach used in `scripts/verify-pr40.mjs` (example body in agent-log entry `2026-04-22 20:00` section). Expect 200 with summary `{filled: 5, rejected: 0, errored: 0}`.

5. **Verify on `/paper`** — one GET to `/api/paper/accounts` OR a visual check, confirm 5 new open positions with the right symbols + quantities.

6. **Report back to user** — show the per-row fills (trade_id + fill_price) + the resulting cash/equity snapshot. Ask whether to leave positions open or reset to pristine.

7. **Delete any throwaway script** you wrote per the one-shot-scripts convention. Do NOT commit `scripts/run-top5-anything.mjs` — only commit if it becomes a reusable tool (it won't).

### Gotchas to remember

- **Playwright DNS flakes on Windows** — if using Playwright for the auth session: first `page.goto()` may hit `ERR_NAME_NOT_RESOLVED`. Retry once.
- **Matrix direction vs user intent** — `entry.direction` for gainers is `"SHORT"` (mean-reversion bet that they'll fall back). Don't blindly copy that to the batch payload if the user said "buy" — clarify.
- **Deviation band** — if today's prices_daily close for one of the top-5 has moved >20% from `entry_price`, the order will reject with `SYNTHETIC_DEVIATION_TOO_LARGE`. Ranking by yesterday's move means these are volatile names; plan for the possibility that 1-2 of 5 fail the band.
- **`bd` not set up in this repo** — ignore the "run bd sync" step from the skill template. Just use git.
- **`.claude/sessions/` and `.claude/agent-log.md` are gitignored**. Use `git add -f` to commit them.
- **GitHub CLI TLS flakes** — for merges, fallback: `curl -sSf -X PUT -H "Authorization: Bearer $(gh auth token)" -H "Accept: application/vnd.github+json" -d '{"merge_method":"squash"}' "https://api.github.com/repos/filippmiller/trading/pulls/N/merge"`

---

**Start by:**
1. `cd C:\dev\trading`
2. `git checkout master && git pull --ff-only origin master`
3. Read `.claude/SYSTEM.md` for topology
4. Read `.claude/agent-log.md` top 3 entries for recent context
5. Ask user: "Do you still want the top-5 gainers added to paper trade? Last session deferred it after a Playwright issue."
6. If yes → execute Priority Order above.
